# Learned Lessons: Integration Gotchas

> Auto-maintained by MetaClaw Learning System. Do not edit manually unless pruning.
> This file accumulates lessons learned from integration failures as JARVIS encounters them.

---

## Template Entry

Each gotcha entry should follow this format:

```markdown
## [Platform/Library] — [Short Name of the Gotcha]

**Error:** [exact error message that was thrown]

**Cause:** [the underlying reason — not just the symptom]

**Rule:** [one-sentence rule to prevent it next time]

**Fix:**
```code
// ❌ Broken pattern
...

// ✅ Correct pattern
...
```

**What broke:** [the original context — what was being attempted and why the wrong approach seemed natural]
```

---

<!-- Lessons will be appended here as JARVIS encounters and recovers from integration failures -->
